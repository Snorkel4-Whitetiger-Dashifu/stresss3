SOC operations needs the broken on-call escalation rollup at `/app/workflow/export_report.py` diagnosed and repaired. Use the noisy archive at `/app/incident/export_dossier.md` to substantiate findings, the frozen source at `/app/workflow/.export_report.original` for pipeline evidence, and `/app/docs/report_spec.json` as the authoritative acceptance contract. Do not alter the frozen source.

Create `/app/log_audit.py` with the documented `diagnose` and `repair` interfaces. Diagnosis must report every allowed issue using verbatim evidence from the supplied sources. Repair must restore deterministic behavior for the primary input and alternate inputs, preserve the requested output directory, and leave `summary.json`, `service_matrix.json`, `flagged.jsonl`, `diagnosis.json`, and `repair_audit.json` there. The repaired workflow itself must support the `--input` and `--output-dir` interface defined by the spec.

Treat schema details as exact contracts rather than examples. In particular, `repair_audit.json.processing_steps` must equal the spec’s ordered array verbatim; audit token maps must use the literal keys supplied by the spec; and pre-repair evidence must be captured before the workflow is changed. Before finishing, run `python3 /app/log_audit.py repair --output-dir /app/output` so the default deliverables are populated, while retaining support for custom output directories.
