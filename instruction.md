Restore the SOC escalation rollup at `/app/workflow/export_report.py`. Build `/app/log_audit.py` with `diagnose --dossier PATH --report PATH` and `repair --output-dir PATH`, using `/app/incident/export_dossier.md` and `/app/workflow/.export_report.original` to substantiate the six required findings.

Evidence must be verbatim and case-sensitive: dossier quotes are at least 30 characters, while pipeline evidence and repair actions are at least 10. Pipeline evidence and the pre-repair SHA come from the frozen original bytes; token maps use the exact literals `event["observed_at"]` and `severity == "critical"`. Never modify the frozen file.

The repaired export must generalize to alternate input while handling alert canonicalization, deterministic deduplication, escalation overrides, compact JSONL, and transitive campaign correlation across related alerts. `/app/docs/report_spec.json` is the operational acceptance contract for schemas, correlation behavior, deterministic ordering, and digest serialization.

Every repair invocation must reinstall and run the corrected workflow, then rerun it for idempotency. Produce exactly `summary.json`, `service_matrix.json`, `flagged.jsonl`, `diagnosis.json`, and `repair_audit.json`; `post_repair` contains integer `escalated_count` and `rerun_escalated_count`. Finish with `python3 /app/log_audit.py repair --output-dir /app/output` so the default diagnosis remains in repaired mode.
