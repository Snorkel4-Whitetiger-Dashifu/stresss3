SOC operations needs the broken on-call escalation rollup at `/app/workflow/export_report.py` diagnosed and repaired. Use the noisy archive at `/app/incident/export_dossier.md` to substantiate findings and the frozen source at `/app/workflow/.export_report.original` for literal pipeline evidence. Do not alter that frozen source.

Create `/app/log_audit.py` with `diagnose --dossier PATH --report PATH` and `repair --output-dir PATH`. Diagnosis must report exactly `wrong_observed_field`, `severity_filter`, `recency_order`, `severity_normalization`, `dedupe_alert`, and `muted_filter`; evidence excerpts are verbatim, and the required evidence terms in the contract are case-sensitive.

Diagnose mode sets `pipeline_status` to `diagnosed`, reports `input_stats` with exactly `alert_count`, `unique_alert_ids`, and sorted normalized `asset_groups`, and omits `verified_summary` and `output_paths`. Repair mode sets status to `repaired`, embeds a complete copy of `summary.json` as `verified_summary`, and uses the semantic path keys `summary_json`, `flagged_jsonl`, and `service_matrix_json`.

Restore the operational pipeline by normalizing timestamps, severity, asset groups, signatures, and muted values; deduplicating alerts through the complete observed-time/severity/muted/signature/asset-group cascade; and escalating unmuted high/critical alerts in recency order. Canonical escalation overrides are scoped by asset group plus `all` or matching severity, use half-open windows, merge overlapping or touching intervals, suppress matching candidates, and contribute independent 121ms `all` and severity probe pressure. Escalation rows carry the 12-character SHA-1 digest defined by the contract and must be emitted as compact JSONL.

The override checksum uses canonical rows `asset_group|severity_scope|start_ms|end_ms`, ordered by normalized key and interval, joined by LF with no trailing newline, and hashed as UTF-8 SHA-256. `/app/docs/report_spec.json` remains normative for the remaining exact schemas, digest payloads, formulas, and ordering.

Repair must be self-contained: capture audit evidence from the frozen original, reinstall the corrected workflow on every invocation even if the broken file was restored, run it in the requested output directory, and rerun it for idempotency. Leave exactly `summary.json`, `service_matrix.json`, `flagged.jsonl`, `diagnosis.json`, and `repair_audit.json` there; the workflow must also support alternate `--input` and custom `--output-dir`.

`repair_audit.json.processing_steps` equals the contract array verbatim, and token maps use its exact literal keys. Before finishing, run `python3 /app/log_audit.py repair --output-dir /app/output`.
